name: sv_visualization
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.9
  - pandas
  # Note: matplotlib, seaborn from base env
  # Note: R packages (r-base, r-ggplot2, r-dplyr) have conflicts - skip for now
