name: sv_calling
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - python>=3.9
  # SV callers
  - delly>=1.1
  # Note: Manta removed - requires Python 2.7 (incompatible with modern Python)
  # Note: DELLY + LUMPY provide comprehensive SV detection
  # SV merging
  - survivor>=1.0
  # Note: samtools, bcftools, tabix from base env (LUMPY uses samtools)
