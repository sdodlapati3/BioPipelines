name: sv_calling
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - python>=3.9
  # Note: All SV callers removed due to dependency conflicts:
  #   - Manta: requires Python 2.7
  #   - Delly: libdeflate/htslib conflicts
  #   - SURVIVOR: python version conflicts
  # Note: Use LUMPY + samtools from base env
  # Note: Python scripts for merging/visualization
