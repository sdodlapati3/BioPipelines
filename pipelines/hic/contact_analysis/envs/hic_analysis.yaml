name: hic_analysis
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  # Core tools only - ultra-minimal
  - cooler>=0.9
  - hicexplorer>=3.7
  - python>=3.10
  - numpy
  - pandas
  # Note: h5py, scipy, scikit-learn, matplotlib, seaborn from base env
  # Note: cooltools, chromosight may have conflicts - install separately if needed
