name: hic_processing
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  # Hi-C specific tools only
  - hic-straw>=1.3
  - pairtools>=1.0
  - pairix>=0.3
  - cooler>=0.9
  - hicexplorer>=3.7
  
  # Utilities
  - python>=3.10
  - numpy
  - pandas
  # Note: bowtie2, samtools, bwa, bedtools, bgzip, tabix, scipy, pysam from base env
