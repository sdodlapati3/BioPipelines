name: methylation_viz
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  # Python visualization (minimal for faster resolution)
  - python=3.10
  - matplotlib=3.7
  - seaborn=0.12
  - pandas=2.0
  - numpy
  - scipy
