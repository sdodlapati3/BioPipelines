name: methylation_bismark
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  # Core bisulfite sequencing tools - ultra minimal
  - python>=3.9
  - bismark>=0.24
  - bowtie2>=2.5
  # Skip problematic tools, install separately if needed
  # - trim-galore
  # - cutadapt  
  # - samtools
